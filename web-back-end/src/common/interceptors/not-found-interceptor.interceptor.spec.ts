import { NotFoundInterceptorInterceptor } from './not-found-interceptor.interceptor'

describe('NotFoundInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new NotFoundInterceptorInterceptor()).toBeDefined()
  })
})
